This repositoy requires:
  - gcc 8 or higher
  - cmake 3.0 or higher

To build the exercises:
1) sudo yum install cmake
2) export PATH=/opt/rh/devtoolset-8/root/usr/bin:$PATH
3) cd exercises
4) mkdir build
5) cmake ../
6) make
